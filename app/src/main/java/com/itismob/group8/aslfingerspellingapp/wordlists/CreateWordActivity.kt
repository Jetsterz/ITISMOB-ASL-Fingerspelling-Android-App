package com.itismob.group8.aslfingerspellingapp.wordlists

import androidx.appcompat.app.AppCompatActivity

class CreateWordActivity : AppCompatActivity() {
//ask if want camera or no camera
    //if yes camera, capture gesture
    //else, manually type word details
}