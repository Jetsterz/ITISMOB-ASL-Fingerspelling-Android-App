package com.itismob.group8.aslfingerspellingapp.wordlists

import androidx.appcompat.app.AppCompatActivity

class EditWordActivity : AppCompatActivity() {
//add video creation feature
    //if opt-out of video, don't open camera, just type word (auto-deletes video that exists)
    //if video opt-in, capture gesture of video
}